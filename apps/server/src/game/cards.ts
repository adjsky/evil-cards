export const whiteCards: string[] = Array.from({ length: 500 }).map(
  (_, index) => `белая карта ${index + 1}`
)
export const redCards: string[] = Array.from({ length: 500 }).map(
  (_, index) => `красная карта ${index + 1}`
)
